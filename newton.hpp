#ifndef NEWTON_HPP
#define NEWTON_HPP

void main_routine(
                  double alpha,
                  double l1_start,
                  double l2_start,
                  double l3_start,
                  double start_h,
                  double max_runge_error,
                  double eps,
                  double delta,
                  int step_count
                 );

#endif